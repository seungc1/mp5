version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - cd frontend && npm install

  build:
    commands:
      - npm run build        # cd frontend 완전히 제거!

artifacts:
  base-directory: dist       # frontend/ 제거! 이미 안에 있으니까
  files:
    - '**/*'